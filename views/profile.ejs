<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login page</title>
</head>

<body>
    <main class="bg-zinc-900 min-h-screen w-full text-white flex flex-col">
        <div class="flex justify-between px-4 py-2">
            <a class="text-blue-700" href="/">Go to Home</a>
            <a class="inline-block bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                href="/logout">Logout</a>
        </div>
        <div class="flex  flex-col">
            <div class="text-center flex-col">
                <h1 class="text-2xl font-bold mb-4"></h1>
                <h2 class="text-3xl mb-2">Hello, <span class="text-blue-400">
                        <%= user.name %>
                    </span></h2>
                <p class="text-xs mb-4">You can create a new post</p>
                <form class="flex justify-center items-center gap-5" action="/post" method="post">
                    <textarea
                        class="w-1/3 px-4 py-2 resize-none bg-transparent border border-gray-400 rounded-md focus:outline-none focus:border-blue-500"
                        name="content" id="" placeholder="What's on your mind?"></textarea>
                    <input type="submit" value="create post"
                        class="cursor-pointer mt-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"></input>

                </form>

            </div>
            <div class=" p-6 rounded shadow-md">
                <h4 class="text-xl font-bold mb-4">Posts</h4>
                <div class="post-container flex flex-wrap gap-5 mt-4">

                    <% user.posts.reverse().map(post=> { %>
                        <div
                            class="post1 max-h-md  bg-zinc-800 border w-[31%] border-gray-500 p-4 mb-4">
                            <h5 class="text-xs text-blue-500 font-semibold">
                                <%= user.username %>
                            </h5>
                            <p class="inline-block  max-h-[50%]  overflow-auto text-gray-200  text-sm">
                                <%= post.content %>
                            </p>
                            <div class="text-xs mt-4">
                                <%= post.likes.length %> likes
                            </div>
                            <div class="flex gap-4 mt-2">
                                <a class="text-pink-400 font-semibold" href="/like/<%=  post._id %>">
                                    <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>

                                </a>
                                <a class="text-zinc-400" href="/edit/<%= post._id  %>">edit</a>
                                <a class="ml-60 text-red-400" href="/delete/<%= post._id  %>">delete</a>
                            </div>
                        </div>
                        <% }); %>


                            <!-- Add more posts here -->
                </div>
            </div>

        </div>



    </main>


</body>

</html>