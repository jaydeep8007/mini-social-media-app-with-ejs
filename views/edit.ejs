<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login page</title>
</head>

<body>
    <main class="bg-zinc-900 min-h-screen w-full text-white flex flex-col">
        <div class="flex justify-between px-4 py-2">
            <a class="text-blue-700" href="/">Go to Home</a>
            <a class="inline-block bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                href="/logout">Logout</a>
        </div>
        <div class="flex  flex-col">
            <div class="text-center flex-col">
                <h1 class="text-2xl font-bold mb-4"></h1>

                <p class="text-xs mb-4">edit your post here.</p>
                <form action="/update/<%= post._id  %>" method="post">
                    <textarea
                        class="w-1/3 px-4 py-2 resize-none bg-transparent border border-gray-400 rounded-md focus:outline-none focus:border-blue-500"
                        name="content" id=""><%= post.content%></textarea>
                    <input type="submit"
                        class="mt-4 bg-yellow-500 hover:bg-yellow-700 text-black font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                        value="update"></input>

                </form>

            </div>


        </div>



    </main>


</body>

</html>